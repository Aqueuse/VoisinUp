using Microsoft.AspNetCore.Mvc;

namespace VoisinUp.Controllers;

public class QuestController : Controller {
    // ADD quest
    
    // DELETE quest
    
    // GET quest (questId)
    
    // GET voisinageQuests (voisinageId)
    
    // GET userQuests (userId)
    
    // UpdateStatusQuest (questId)
        // give asset
        // maybe give success
    
    // SetQuestOwnerId
}